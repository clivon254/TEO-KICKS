# Configure npm to handle optional dependencies properly
optional=false
legacy-peer-deps=true
# Force npm to use the pure JavaScript implementation
rollup-native=false
# Disable native modules
node-linker=hoisted
# Force pure JavaScript builds
build-from-source=false 